//! `StatefulAdjudicator` implementation: P0 coarse intent classification and
//! declaration/behavior consistency; P1 adds session drift detection; the
//! plugin interface stays unchanged across phases (§6.2).
