# Skill Security Config
# System install path: /etc/agent-sec/skill-security/config.conf
# Trusted keys default to the sibling trusted-keys/ directory.
# trusted_keys_dir = /etc/agent-sec/skill-security/trusted-keys

# Skills directories to verify (one per line)
skills_dir = [
    /usr/share/anolisa/skills
]
